# Ignore all data; track only metadata (CLAUDE.md, README* files)
*
!.gitignore
!CLAUDE.md
!README*
!*/

# Never track HF download cache or metadata sidecar files
.cache/
*.metadata

# test fixture (small, already present)
!/test_fire_100/
