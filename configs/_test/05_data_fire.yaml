# Dataset config — Fire Detection (100 images for pipeline testing)
dataset_name: "test_fire_detection_100"
path: "../../dataset_store/test_fire_detection_100"
train: "train/images"
val: "val/images"
test: "val/images"

names:
  0: fire
  1: smoke
num_classes: 2
input_size: [640, 640]

mean: [0.485, 0.456, 0.406]
std: [0.229, 0.224, 0.225]

text_prompts:
  fire: "flames, burning fire, visible fire"
  smoke: "smoke, gray or white smoke plume, haze from fire"
